import board
import busio
import pwmio

clk = pwmio.PWMOut(board.GP20, frequency=8000000, duty_cycle = 32768)

i2c = busio.I2C(board.GP9, board.GP8)

while not i2c.try_lock():
    pass

print ([hex(x) for x in i2c.scan()])

i2c.unlock()
